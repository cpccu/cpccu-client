export const metadata = {
    title: 'CPCCU Admin Panel',
    description: 'Administrative dashboard for the CPCCU Programming Club',
};
export default function AdminRootLayout({ children, }) {
    return <>{children}</>;
}

services:
  - type: web
    name: next-js
    runtime: node
    plan: starter
    buildCommand: bun install; bun run build
    startCommand: bun run start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
